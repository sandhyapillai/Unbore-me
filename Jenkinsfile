// Pipeline for building and deploying 
 node {
        stage('Checkout') {
            checkout scm
        }
        stage('Main') {
            sh 'echo test'
        }
        stage('Post') {
            sh 'echo test'
        }
    }
